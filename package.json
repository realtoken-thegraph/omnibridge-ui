{
  "name": "@omnibridge/subgraph",
  "version": "1.0.0",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.2",
    "@graphprotocol/graph-ts": "^0.31.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "license": "MIT",
  "scripts": {
    "auth": "graph auth https://api.thegraph.com/deploy/ $GRAPH_ACCESS_TOKEN",
    "prepare-mainnet": "mustache config/mainnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare-xdai": "mustache config/xdai.json subgraph.template.yaml > subgraph.yaml",
    "prepare-goerli": "mustache config/goerli.json subgraph.template.yaml > subgraph.yaml",
    "prepare-sepolia": "mustache config/sepolia.json subgraph.template.yaml > subgraph.yaml",
    "load": "ts-node scripts/loadConstants.ts",
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "test": "graph test",
    "deploy-goerli": "yarn prepare-goerli && yarn load goerli && yarn codegen && yarn build && graph deploy realtoken-thegraph/amb-goerli --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-sepolia": "yarn prepare-sepolia && yarn load sepolia && yarn codegen && yarn build && graph deploy --studio omnibridge-amb",
    "deploy-mainnet": "yarn prepare-mainnet && yarn load mainnet && yarn codegen && yarn build && graph deploy realtoken-thegraph/bridge-eth --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-xdai": "yarn prepare-xdai && yarn load xdai && yarn codegen && yarn build && graph deploy realtoken-thegraph/bridge-gnosis --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.20",
    "matchstick-as": "^0.5.2",
    "mustache": "^4.2.0"
  }
}
